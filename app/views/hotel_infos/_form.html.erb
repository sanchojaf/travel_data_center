<%= form_for(@hotel_info) do |f| %>
  <% if @hotel_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hotel_info.errors.count, "error") %> prohibited this hotel_info from being saved:</h2>

      <ul>
      <% @hotel_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
